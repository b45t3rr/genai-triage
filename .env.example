# =============================================================================
# CONFIGURACIÓN MULTI-MODELO LLM
# Configure al menos una API key para usar la aplicación
# =============================================================================

# Proveedor por defecto (OPCIONAL)
DEFAULT_MODEL_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-nano
OPENAI_TEMPERATURE=0.1

# XAI (Grok) Configuration
XAI_API_KEY=your_xai_api_key_here
XAI_MODEL=grok-beta
XAI_TEMPERATURE=0.1

# Google Gemini Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-pro
GEMINI_TEMPERATURE=0.1

# DeepSeek Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TEMPERATURE=0.1

# Anthropic Claude Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-sonnet-20240229
ANTHROPIC_TEMPERATURE=0.1

# Configuración de la aplicación (OPCIONAL)
APP_NAME=PDF Report Analyzer
APP_VERSION=1.0.0
DEBUG=false

# Configuración de archivos (OPCIONAL)
MAX_FILE_SIZE_MB=50
SUPPORTED_EXTENSIONS=[".pdf"]

# =============================================================================
# CONFIGURACIÓN ADICIONAL
# =============================================================================

# =============================================================================
# CONFIGURACIÓN MONGODB
# Para usar --mongodb, configure al menos MONGODB_URI
# =============================================================================

# MongoDB Configuration
# For local development (MongoDB running locally)
#MONGODB_URI=mongodb://localhost:27017/
# For Docker environment (using docker-compose.yml)
MONGODB_URI=mongodb://admin:password123@mongodb:27017/

# MongoDB Docker Configuration (used by docker-compose.yml)
MONGO_USERNAME=admin
MONGO_PASSWORD=password123
MONGO_DATABASE=vulnerability_validation

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Custom database and collection names
DATABASE_NAME=vulnerability_validation
COLLECTION_NAME=assessments